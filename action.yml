name: 'Contribution Tracker'
description: 'Captures GitHub contribution events and emits a standardised payload'
author: 'your-name'

inputs:
  github-token:
    description: 'GitHub token for API calls'
    required: true

runs:
  using: node24
  main: dist/index.js
